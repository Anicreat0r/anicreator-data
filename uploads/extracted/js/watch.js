const $=s=>document.querySelector(s),p=new URLSearchParams(location.search),key=p.get('anime'),sn=Number(p.get('season')||1),en=Number(p.get('ep')||1);let anime,season,eps=[],idx=0,lang='sub';const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const link=e=>`watch.html?anime=${encodeURIComponent(anime.slug||anime.id)}&season=${encodeURIComponent(season.season_number)}&ep=${encodeURIComponent(e.episode_number)}`;function fail(x){$('#loading').classList.add('hidden');$('#error').textContent=x}function render(){let e=eps[idx];if(!e)return fail('Episode not found.');let movie=(anime.type||'anime')==='movie';document.title=`${anime.title} - ${e.title||'Episode '+e.episode_number}`;$('#back').href=movie?'movie.html':'anime.html';$('#title').textContent=e.title||`${movie?'Movie':'Episode '+e.episode_number}`;$('#meta').textContent=movie?anime.title:`${anime.title} • Season ${season.season_number} • Episode ${e.episode_number}`;$('#actions').classList.toggle('hidden',movie);$('#tabs').classList.toggle('hidden',!e.sub_url&&!e.dub_url);let u=lang==='dub'?(e.dub_url||e.sub_url):(e.sub_url||e.dub_url);if(u){$('#frame').src=u;$('#frame').classList.remove('hidden');$('#empty').classList.add('hidden')}else{$('#frame').removeAttribute('src');$('#frame').classList.add('hidden');$('#empty').classList.remove('hidden')}document.querySelectorAll('.lang').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));$('#prev').disabled=idx<=0;$('#next').disabled=idx>=eps.length-1;$('#episodes').innerHTML=movie?'':eps.map((x,i)=>`<a class="episode${i===idx?' current':''}" href="${link(x)}"><div class="epno">EP ${x.episode_number}</div><div class="eptitle">${esc(x.title||'Episode '+x.episode_number)}</div></a>`).join('');$('#loading').classList.add('hidden');$('#page').classList.remove('hidden')}function move(d){let i=idx+d;if(i<0||i>=eps.length)return;location.href=link(eps[i])}$('#prev').onclick=()=>move(-1);$('#next').onclick=()=>move(1);document.querySelectorAll('.lang').forEach(b=>b.onclick=()=>{lang=b.dataset.lang;render()});document.onkeydown=e=>{if(e.target.matches('input,textarea,select'))return;if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)};async function init(){try{if(!key)throw Error('No content specified.');let r=await fetch(`data/anime/${encodeURIComponent(key)}.json`,{cache:'no-cache'});if(!r.ok)throw Error('Content not found.');anime=await r.json();season=(anime.seasons||[]).find(s=>Number(s.season_number)===sn)||anime.seasons?.[0];if(!season)throw Error('Season not found.');eps=[...(season.episodes||[])].sort((a,b)=>Number(a.episode_number)-Number(b.episode_number));idx=Math.max(0,eps.findIndex(e=>Number(e.episode_number)===en));render()}catch(e){fail(e.message)}}init();
