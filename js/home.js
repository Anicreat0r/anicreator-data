const $=s=>document.querySelector(s);const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const link=e=>`watch.html?anime=${encodeURIComponent(e.anime_slug)}&season=${encodeURIComponent(e.season_number)}&ep=${encodeURIComponent(e.episode_number)}`;const card=e=>`<a class="card" href="${link(e)}"><img class="episode-thumb" src="${esc(e.thumbnail||e.poster||'')}" loading="lazy" alt="${esc(e.anime_title)}"><div class="card-body"><div class="card-title">${esc(e.anime_title)}</div><div class="card-meta">S${e.season_number} • EP ${e.episode_number}${e.title?' • '+esc(e.title):''}</div></div></a>`;let pool=[];function random(){let a=pool.slice().sort(()=>Math.random()-.5);$('#random').innerHTML=a.slice(0,12).map(card).join('')||'<div class="state">No random episodes available.</div>'}async function init(){try{let r=await fetch('data/home.json',{cache:'no-cache'});if(!r.ok)throw Error('Could not load home.json');let d=await r.json();$('#latest').innerHTML=(d.latest||[]).slice(0,12).map(card).join('')||'<div class="state">No updates yet.</div>';pool=d.random||[];random()}catch(e){$('#random').innerHTML=`<div class="state error">${esc(e.message)}</div>`}}$('#shuffle').onclick=random;$('#globalSearch').onkeydown=e=>{if(e.key==='Enter'&&e.target.value.trim())location.href=`anime.html?q=${encodeURIComponent(e.target.value.trim())}`};init();
