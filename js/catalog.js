const $=s=>document.querySelector(s);const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));let all=[];function filtered(q){q=q.toLowerCase().trim();return all.filter(a=>`${a.title||''} ${a.genres||''} ${a.tags||''}`.toLowerCase().includes(q))}function render(list){$('#grid').innerHTML=list.length?list.map(a=>`<a class="card" href="anime-details.html?anime=${encodeURIComponent(a.slug||a.id)}"><img class="cover" src="${esc(a.poster||'')}" loading="lazy" alt="${esc(a.title||'')}"><div class="card-body"><div class="card-title">${esc(a.title||'Untitled')}</div><div class="card-meta">${window.CONTENT_TYPE==='movie'?'Movie':esc(a.status||'Anime')}</div></div></a>`).join(''):'<div class="state">Nothing found.</div>';$('#count').textContent=`${list.length} ${window.CONTENT_TYPE==='movie'?'movies':'anime'}`}async function init(){try{let r=await fetch('data/anime-index.json',{cache:'no-cache'});if(!r.ok)throw Error('Could not load anime-index.json');all=(await r.json()).filter(a=>(a.type||'anime').toLowerCase()===window.CONTENT_TYPE);let q=new URLSearchParams(location.search).get('q')||'';$('#search').value=q;$('#loading').classList.add('hidden');render(filtered(q))}catch(e){$('#loading').classList.add('hidden');$('#error').textContent=e.message}}$('#search').oninput=e=>render(filtered(e.target.value));init();
